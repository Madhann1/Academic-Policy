[build]
  # This config matches a "Base Directory: frontend" setup in Netlify UI
  command   = "npm run build"
  publish   = "dist"

# React Router SPA redirect — all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
