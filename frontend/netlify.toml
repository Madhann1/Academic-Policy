[build]
  base      = "."
  command   = "npm run build"
  publish   = "dist"

# React Router SPA redirect — all routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
